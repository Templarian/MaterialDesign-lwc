import { LightningElement } from 'lwc';

/**
 * @title Basic
 * @subtitle No Attributes
 * @description Passing no attributes or an empty `path` produces a default box.
 */
export default class Basic extends LightningElement {}