import { LightningElement, track, api } from 'lwc';

/**
 * Render overlapping child `mdi-icon` elements.
 * 
 * @name Stack
 * @order 2
 */
export default class Stack extends LightningElement {
    
}
